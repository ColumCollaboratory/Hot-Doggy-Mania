using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace HotDoggyMania.SceneLogic.MovementSystemTest
{
    /// <summary>
    /// Defines the logic used in the test scene for the movement system.
    /// This logic is strictly for testing purposes.
    /// </summary>
    public sealed class MovementSystemTestInteraction : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
            
        }

        // Update is called once per frame
        private void Update()
        {
        
        }
    }
}
